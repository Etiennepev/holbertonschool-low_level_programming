#include "main.h"
/**
 * _strlen - lenght of a strig
 *
 *
 *@s: l'endroit ou est la chaine de caracteres
 *Return: 1 or 0
 */

int _strlen(char *s)
{
	char str[] = {'H', 'e', 'l', 'l', 'o', '\0'};

	_putchar(str);
	return(0);
}
