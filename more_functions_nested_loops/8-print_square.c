#include "main.h"
/**
 * print_diagonal - Print diagonal
 *
 *@n: number of diagonal
 */
void print_square(int size)
{
	int height, width;
	size = 

	if (size <= 0)
	{
		_putchar('\n');
		return;
	}
	for (height = 0; height < size; a++)
	{
		for (width = 0; width < height; b++)
		{
			_putchar('#');
		}
		_putchar('/n')
