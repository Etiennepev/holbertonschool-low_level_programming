#include "main.h"
/**
 * _isupper - check for the uppercase
 * @c: The character to print
 *
 * Return: 1 if uppercase else 0
 *
 */
int _isdigit(int c)
{
	if (c > 47 && c < 58)
	{
		return (1);
	}
	else
	{
		return (0);
}
}
